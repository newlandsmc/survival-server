messages:
  toggle-on:
    - '<green>Staff mode toggled on.'
  toggle-off:
    - '<red>Staff mode toggled off.'
  log-on:
    - '<green>You''re still in staff mode!'
actionbar:
  enable: true
  bar: '<green>You are currently in staff mode!</green>'
